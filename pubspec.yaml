name: anish_portfoilio
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.8.1

dependencies:
  flutter:
    sdk: flutter
  font_awesome_flutter: ^10.4.0
  url_launcher: ^6.1.7
  animated_text_kit: ^4.2.2
  flutter_animate: ^4.3.0
  cupertino_icons: ^1.0.8
  animated_background: ^2.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/profile.png
